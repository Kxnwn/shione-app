import { Request , Response } from "express"
import { getTodayMoodServies } from '../services/verse.services.js'


export const getTodayVerse = async (req: Request, res: Response) => {
    const userId = req.user.id

    try {
     
    } catch (error) {
        
    }
}